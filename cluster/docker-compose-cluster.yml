version: '3'
services:
  minio:
    image: minio/minio:latest
    restart: always
    container_name: minio-cluster
    environment:
      - "MINIO_ACCESS_KEY=AKIAIOSFODNN7EXAMPLE"
      - "MINIO_SECRET_KEY=wJalrXUtnFEMIK7MDENGbPxRfiCYEXAMPLEKEY"
    ports:
      - 9000:9000
    volumes:
      - /minio/data:/minio:rw
      - /minio/config:/root/.minio:rw
    command: server http://192.168.0.1/minio http://192.168.0.2/minio http://192.168.0.3/minio http://192.168.0.4/minio
    network_mode: host 
